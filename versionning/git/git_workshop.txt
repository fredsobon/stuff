=== git workshop ===


= workflow :

première convention de nommage : 
production --> branche : nommer avec prefixe Feature ou Hotfix (elle est créee en WIP dans gitlab --> code --> commit.

concepts :

rm
conflit
rebase / pull
cherry-pick
stash
ammend
squash
tag
fetch
add -i
rebase -i HEAD~2
reset --hard
diff
log
remote

---

git branch toto
git checkout -b toto : creee la branche et rentre dedans

git checkout -b tata production 
git checkout -b tata origin/production   <<< on peut creer une branche prenant origin/prod en origin


git fetch -a : recupere toutes les modifs faites sur le remote 






