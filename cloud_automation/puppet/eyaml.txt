=== notes eyaml : ===


on va chiffrer un mot x fois avec les clés pub de tout le monde et la clé du serveur pupppet master
sur le server puppet master on aun fichier recipients qui contient les fingerprint des clé pub.

on va déchiffrer avec notre clé privées


on ne va chiffrer qu'une entrée d'un fichier pas forcement tout le fichier.

eyaml permet de chiffrer et déchiffrer des données.

on va chiffrer automatiquement les données.
