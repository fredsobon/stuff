


- installation plugin helm : 

helm plugin install https://github.com/databus23/helm-diff --version master


- charger un plugin a une version speciale :
sur github on peut dans l'onglet tag choisir notre version 

helm plugin install https://github.com/databus23/helm-diff --version 3.0.0-rc.7
